import styled from "styled-components";

export const ModalStyle = styled`
  position: 'absolute' as 'absolute';
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-radius: 4px;
  width: auto;
  height auto;
  filter: drop-shadow(8px 4px 4px rgba(0, 0, 0, 0.25));
  background: 'background.paper'
`
